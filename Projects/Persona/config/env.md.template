# Environment Variables

## Default Provider
default_provider: claude

## Provider Paths
# Update these paths to match your system
claude_path: /path/to/claude
gemini_path: gemini
jules_path: jules

## Provider-Specific Settings
# Claude: Uses --print --dangerously-skip-permissions --model
# Gemini: Uses --sandbox false, native AGENTS.md support
# Jules: Uses task create --description, async execution

## Outlook (Optional - for calendar integration)
# username: your-email@example.com
# password: (leave blank, use OAuth)
